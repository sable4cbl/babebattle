import type { EffectScript } from "../../types/effects";
import { stubEffect } from "./helpers";

const names = [
  "Captured MILF",
  "MILF Duo",
  "Mommys Girl",
  "Sexual Experience",
  "Stepmom Is Stuck",
  "Still Got It",
  "Vintage Form",
];

export const MILF_EFFECTS: EffectScript[] = names.map((name) =>
  stubEffect("MILF", name, `EFFECT MILF ${name}.gif`)
);

